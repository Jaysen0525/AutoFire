# APK 安全验证说明

> 给准备安装这个 APK 的人看。这里列出的每一条**都可以自己动手验证**，
> 不需要相信作者的任何口头承诺。

---

## 一、三条硬事实

### 1. 它没有联网权限 —— 数据在物理上出不去

APK 里**不存在 `INTERNET` 权限**。Android 的权限是内核级沙箱：没有这个权限，
进程发起的任何网络请求都会被系统直接拒绝，**不是"作者承诺不上传"，而是"系统不让它上传"**。

> 怎么自己看：装好后进「设置 → 应用管理 → 自动续火花 → 流量使用」，永远是 0；
> 权限列表里也不会出现"网络"或"流量"这一项。

### 2. 它不读取你装过哪些应用

早期版本为拉起多闪用过 `QUERY_ALL_PACKAGES`（读取所有已安装应用的权限），
现已改成**只声明多闪一个包名**：

```xml
<queries>
    <package android:name="my.maya.android" />
</queries>
```

它只能看到多闪，看不到你手机里的其他应用。

### 3. 它申请了哪些权限，以及为什么

| 权限 | 用途 |
|---|---|
| 无障碍服务 | 读取多闪界面、执行点击与输入。**核心能力** |
| 悬浮窗 | 任务执行期间保持屏幕常亮 |
| 通知 / 全屏通知 | 推送执行结果；到点由系统点亮屏幕并唤起解锁 |
| 精确闹钟 | 到点准时执行 |
| 循环唤醒 / 前台服务 | 任务期间防止进程被系统冻结 |
| 开机自启 | 重启后恢复定时 |
| 忽略电池优化 | 避免凌晨被系统限流 |

**全部 10 项都在这里，没有隐藏项。**

---

## 二、自己验证的三种方式

### 方式 A：不装任何工具（最简单）

1. 装好后打开「设置 → 应用管理 → 自动续火花」
2. 查看「权限」——逐项对照上面的表格，**确认没有"网络 / 流量"这一项**
3. 查看「流量使用」——应为 0

### 方式 B：核对文件指纹（推荐）

APK 在传输过程中是否被替换，用哈希值一比对就知道。

**本包指纹（发布时公布，可用于比对任何一次传输）：**

| 项目 | 值 |
|---|---|
| 文件名 | `app-release.apk` |
| 文件大小 | 2,255,934 字节（2.15 MB） |
| **SHA-256** | `15195597AD904429DE81D5FCBBCCF52E90F83AFFB96E6F32CD9D2B2ABDE3FF3C` |
| **签名证书 SHA-256** | `5D:69:1D:9F:35:FF:76:1C:4F:E8:49:97:C5:DD:87:DC:60:2C:98:A1:78:5E:CA:55:04:50:67:8F:A2:8C:87:91` |
| 证书主体 | `CN=AotuFire, OU=Personal Build, O=Personal, L=Home, ST=Home, C=CN` |

**在电脑上算（Windows PowerShell）：**

```powershell
Get-FileHash .\app-release.apk -Algorithm SHA256
```

**在电脑上验证签名（需要 Android SDK 的 apksigner）：**

```bash
apksigner verify --print-certs app-release.apk
```

输出的 `Signer #1 certificate SHA-256 digest` 应该和上表一致。

### 方式 C：在线多引擎扫描

把 APK 上传到 [VirusTotal](https://www.virustotal.com)，会得到 60 多个引擎的扫描结果和一个可分享的报告链接。

> ⚠️ 用 VirusTotal 前请注意：APK 会上传到对方服务器。这个包本身不含你的任何个人数据（好友昵称、密码都存在手机本地，不在 APK 里），但如果你介意，用方式 A/B 即可。

---

## 三、为什么"签名"这件事值得单独说

Android 用数字证书给 APK 签名。**调试版和正式版的差别很大：**

| | 调试签名（debug） | 正式签名（release） |
|---|---|---|
| 证书 | `CN=Android Debug` | `CN=AotuFire, ...` |
| 私钥 | **公开的、密码固定为 `android`** | 只有作者持有 |
| 能否证明来源 | **不能** —— 任何人都能签出同名的伪造包 | 能，指纹唯一 |

所以：**分享时请只使用 `app-release.apk`**。调试包不具备可证明来源的能力，
收到调试包时任何"这是原版"的说法都无法核实。

如果作者以后发布更新，**证书指纹应该保持不变**（同一个密钥签名）。
万一指纹变了，说明换了签名密钥 —— 那时新包无法覆盖安装，必须卸载重装，
遇到这种情况请先找作者确认。

---

## 四、诚实的边界

上面这些都能证明"**这个文件是作者发布的那一个，且它不能联网**"，但有几件事**证明不了**，
你应该知道：

1. **哈希只能证明文件没被篡改，证明不了代码是善意的。** 它防的是"传输途中被人替换"，
   不是"作者写了坏代码"。
2. **VirusTotal 是信誉信号，不是安全证明。** 免杀技术普遍存在；反过来，
   自行签名的正常应用也偶尔会被启发式引擎误报。
3. **无障碍服务本身是一个很强的权限。** 它确实能读取屏幕上的内容、能模拟点击。
   我们能保证的是"读到的内容出不去"（没有网络权限），
   但"是否愿意授予这么强的权限"仍然需要你自己判断。
4. **最彻底的验证方式是读源码。** 整个工程只有 19 个 Java 文件、零第三方依赖，
   任何人都能自己编译一份来比对行为。这是唯一不依赖任何信任的验证方式。

---

## 五、建议的分享方式

把这几样一起给出去，对方就不必"凭感觉相信"：

1. `app-release.apk`（正式签名版）
2. 本文件（含 SHA-256 与证书指纹）
3. 源码（可选，但最有力）

**如果要公开源码**，务必先排除签名密钥 —— 私钥泄漏意味着任何人都能签出
能覆盖安装到你用户手机上的伪造包：

```gitignore
*.jks
*.keystore
keystore.properties
```

> `keystore.properties` 与 `aotufire-release.jks` 属于**私密文件**：
> 请备份好并妥善保管。一旦丢失，你以后就无法再发布能覆盖安装的更新版本
> （用户必须卸载重装）；一旦泄漏，别人可以伪造你的更新包。
