# 自动续火花（AotuFire）开发记录

> 这份文档记录**界面结构实测数据、踩坑记录、设计取舍**，面向继续改代码的人。
> 面向使用者的说明在 `README.md`。

## v2.5：修三个「执行链路深处」的逻辑问题（外部评审发现，全部确认为真）

这三个都是**测试能跑过、但特定条件下会出错**的问题。

### ❶ 75 秒兜底定时器误杀正在执行的任务（严重）

```java
// stepWakeAndUnlock 里挂的兜底
H.postDelayed(() -> { if (!stale(myGen)) finish(false, "解锁超时…"); }, 75_000);
```

这个定时器**从唤醒那一刻开始计时，解锁成功后也没有解除武装**。`stale()` 只判断
"任务是否还在跑 + 代次是否相同"，多好友跑到第 3、4 位时任务当然还在跑 ——
于是 75 秒一到就把**正在执行的任务**掐死。

时间账：解锁约 15 秒 + 每位好友约 15 秒 → 4 位正好 **75 秒**。
上次 4 好友测试刚好压线跑过，纯属侥幸；**换成 5 位必然被腰斩**，
而且是在"前几位已发、后几位没发"的半截状态下被掐断。

修复：引入 `unlockPhase` 标志，兜底定时器**只在解锁阶段才允许开枪**，
解锁一完成（`onUnlocked` / `onUnlockFailed` / 备用通道成功）立刻缴械。

### ❷ 取消路径残留常亮悬浮窗（严重）

`cancel()` 里做了释放唤醒锁、写报告、发通知，**唯独漏了 `ScreenKeeper.stop()`**。
那个 1×1 的常亮悬浮窗会一直挂着 → **屏幕再也无法自动熄灭，一直耗电到进程被杀**。
"卡死任务强制重置"那条路径同样漏了。

修复：两条路径都补上 `ScreenKeeper.stop(appCtx)`。

### ❸ 回读校验假成功（最危险）

```java
public static boolean containsText(svc, text) {
    for (节点 : 所有窗口所有节点)
        if (节点.getText().contains(text)) return true;   // ← 输入框里也有这段文字
}
```

`setInputText` 会把文案写进输入框，**输入框里那段文字同样是一个 text 节点**。
万一发送按钮没点中（被系统吞、坐标偏），文字还留在输入框里 → 校验照样通过 →
任务报告「✔ 成功」，**实际消息根本没发出去**，火花第二天照断。

修复：新增 `containsSentMessage()`，两道过滤：
1. **排除可编辑节点** —— 输入框里的是"还没发出去"，不算数
2. **命中节点必须在输入栏上方**（消息区），排除底部工具栏与输入栏自身

> 教训：**校验逻辑必须验证"目标状态"，而不是"某个字符串存在于界面上"**。
> 后者在"操作只完成了一半"时会给出肯定的假象。

## v2.5：通知图标显示成安卓默认机器人的问题

### 根因一：小图标是一坨实心白圆

`ic_stat_fire.xml` 之前画的是一颗**实心白圆**（`M12,2a10,10 0 1,0 0,20…`）。
24px 下它就是个没有辨识度的圆点，部分 ROM 会把它当成"无法识别的小图标"
而回退成系统默认图标（就是那个安卓机器人）。

修复：换成**火苗轮廓**，并用 `fillType="evenOdd"` 让内焰**镂空** ——
极小尺寸下也能看出是火苗而不是色块。

### 根因二：通知没有指定大图标

不设置 `setLargeIcon` 时，部分 ROM 会自行用"应用图标"或系统默认图标
填充通知右侧那块区域。修复：新增 `Notifier.appIconBitmap()`，
把火苗矢量渲染成 Bitmap 明确指定，三处通知（唤醒 / 前台服务 / 执行结果）全部用上。

### 顺带：图标底色改白

`ic_launcher_background.xml` 从深色炭黑渐变改成**纯白**。
同时把火苗内焰从淡奶油色 `#FFF4CF` 改成琥珀色 `#FFFFB74D` —— 淡色在白底上看不见。

---

# 以下为 v2.4 及之前的记录

## 实测已确认的事实（不是推测）

- 纯本地运行，**不申请 INTERNET 权限**，不上传任何数据
- 零第三方依赖，只用 Android SDK 原生 API
- APK：`app\build\outputs\apk\debug\app-debug.apk`

---

## 实测已确认的事实（不是推测）

| 结论 | 证据 |
|---|---|
| 多闪包名 **`my.maya.android`** | 界面导出报告窗口头 |
| 多闪**不屏蔽无障碍**，节点完全可读 | 报告里能读到 `id/viewpager`、`id/e5u`、`id/ipm` 等深层节点 |
| 多闪控件 id 是**混淆的**（`e`/`kd`/`e5u`/`msa`/`o6y`） | 每次发版都会变 → **定位必须以文字为主，不能依赖 id** |
| **灭屏后能自动亮屏** | 测试 3：`WakeActivity.onCreate 屏幕=亮 锁屏=已锁` |
| **后台启动 Activity 没被小米拦截** | 测试 2：`startActivity` → 16ms 后 Activity 创建 |
| **能自动输入锁屏密码并解锁** | 测试 3：点击数字 6/6 → `requestDismissKeyguard: 成功` → 已解锁 |
| 锁屏数字键盘**在 `requestDismissKeyguard` 之后才出现** | 采集时（t=39s）无键盘，调用后（t=41.8s）才能点到数字 |

**最后一条直接决定了 v1 的实现顺序**：必须先请求系统解锁，再轮询等键盘出现，最后点数字。顺序反了会偶发失败。

---

## 多闪界面结构（实测，2026-09-24）

这些是定位代码的依据。**注意：id 全是混淆的，不能用；下面标 ★ 的才是稳定锚点。**

### 消息列表页

```
顶栏 ViewGroup id/o6y [0,108][1080,221]
  ├ ImageView  id/gzt  D="互动通知"  c          ← 左侧按钮
  ├ DmtTextView id/tv_title  D="消息"           ← ★★ 页面锚点：内容描述=「消息」即在此页
  └ ImageView  id/hg=  D="更多面板"  c

通知横幅（多闪说收不到新消息通知时出现，会顶掉列表位置）
  ├ TextView  id/-y  T="当前无法接收新消息通知  打开通知"
  └ ImageView id/iv_close  c                    ← ★ 可关闭（id 未混淆）

RecyclerView [0,334][1080,2206]                 ← ★ 会话列表（类名 RecyclerView 稳定）
  ├ Button id/n3x [0,334][1080,518]  c          ← ★ 第 1 行 = 置顶好友
  │   ├ FrameLayout id/apo [28,342][197,511]    ← 头像容器
  │   │   ├ DmtTextView id/55  D="好友昵称"      ← ★★ 昵称藏在 contentDescription 里！
  │   │   └ ImageView  id/rhd  D="在线"
  │   ├ LinearLayout id/n32
  │   │   └ View id/tv_title                     ← 注意：这个是空 View，没有文字
  │   ├ TextView id/hdo T="点亮中 1/3"            ← 火花状态
  │   └ View id/divider
  ├ Button id/n3x [0,518][1080,702]  c          ← 第 2 行，每行固定 184px 高
  └ …（行内还可能嵌套 Button id/eiy D="续火花" —— 多闪自带的一键续火花入口）
```

底部 tab（可用来从任何页面切回消息列表）：

```
FrameLayout [0,2207][360,2358]   c  → LinearLayout id/o_2 D="多闪，按钮"  → TextView id/o_3 T="多闪"
FrameLayout [360,2207][720,2358] c  → LinearLayout id/o_2 D="发现，按钮"  → TextView id/o_3 T="发现"
FrameLayout [720,2207][1080,2358] c → LinearLayout id/o_2 D="我，按钮"    → TextView id/o_3 T="我"
```

### 由此确定的三条定位规则

| 要判断的事 | 稳定锚点 |
|---|---|
| 在不在消息列表页 | 是否存在 contentDescription == `"消息"` 的节点 |
| 哪个是目标会话 | RecyclerView 下按 y 排序的第 N 行 Button，行内**面积最大的 contentDescription** 就是昵称 |
| 怎么切回消息列表 | 点底部的 `D="多闪，按钮"` 那个 tab |

> 早期版本用「屏幕高度比例」框定列表区域（top > 360px），结果**置顶那一行的昵称节点在 y=354，
> 正好被过滤掉** → 会跳过置顶好友去匹配第二行。结构锚定后这个坑不复存在。

### 聊天窗口页

```
顶栏好友昵称    TextView  id/o9e     T="好友甲"        [254,118][407,168]   ← ★★ 收件人锚点（text 字段）
消息列表        RecyclerView id/l5n  [0,0][1080,2085]
底部工具条      RecyclerView id/nmz  [0,2085][1080,2193]
输入框          EditText  id/msg_et                    [134,2219][787,2332]  ← ★★ id 未混淆
发送按钮        ImageView id/gxi     D="发送"           [900,2219][1059,2332]  ← ★★ 描述就是「发送」
```

**注意：聊天页打开后，会话列表仍在同一棵节点树里**（实测能看到
`Button D="置顶,好友甲,[草稿] 我要..."`）。所以「发送前校验收件人」不能用全树搜昵称，
否则在列表页也会命中、闸门形同虚设。可靠判据是：

| 位置 | 昵称存法 | 屏幕位置 |
|---|---|---|
| 聊天页顶栏（真收件人） | **text 字段** | y ≈ 118（顶部 10% 内） |
| 会话列表行（不是收件人） | **contentDescription** | y ≥ 221 |

所以校验规则 = **「text 字段匹配」且「位于屏幕顶部 10% 内」**，两者兼备。

> 另一个观察：会话列表里**只有部分行**暴露了昵称（实测 11 行里只有置顶那一行有名字，
> 其余行的头像节点压根没有 contentDescription）。所以用昵称定位时，如果目标不是置顶行、
> 名字又没暴露，会**匹配失败而不是乱猜** —— 这正是我们要的安全方向。

---

## 踩坑记录

### 坑 1：静态 Handler + onDestroy，导致解锁流程静默死掉（v1.1 修复）

```java
private static final Handler H = new Handler(Looper.getMainLooper());  // ❌ 静态共享

@Override protected void onDestroy() {
    H.removeCallbacksAndMessages(null);   // 清掉整个 Handler 上所有挂起的任务
    super.onDestroy();
}
```

`H` 是所有实例共享的静态 Handler，`onDestroy` 里这一句会把**整条解锁流程后续的每一步**都清掉：
不打日志、不抛异常、不回调。上层 TaskRunner 只能干等到超时，现象就是「手机亮了、然后什么都没发生」。

修复：Handler 改为**实例字段** + 每一步包 `try/catch` 转成明确失败 + **独立看门狗**（50 秒无结论就主动上报失败，不让上层干等）+ `onDestroy` 时若未完成则主动上报。

### 坑 2：诊断包缺了最关键的一份日志（v1.1 修复）

v1.0 分享的包里只有任务报告 + 界面 dump，**没有解锁面板的报告和完整运行日志**。
结果出问题时只能靠推理。现在诊断包含：任务报告 + 解锁面板报告 + 完整运行日志 + 每一步的界面 dump。

### 坑 3：昵称为空会自动退化成「取最上面一条」

没填昵称时无法校验目标，一旦置顶被挤掉或识别有偏差就可能**发给别人**。
现在保存配置时会弹窗警告；填了昵称则只认昵称，认不出宁可失败。

### 坑 4：全屏意图通知「抢跑」，启动了一个不带任务标志的解锁面板（v1.2 修复）

唤醒环节我同时用了两招：屏幕唤醒锁 + 全屏意图通知 + 直接 `startActivity`。而全屏意图通知的
PendingIntent 里**漏了 `EXTRA_FOR_TASK` 标志**，于是：

```
21:15:00.118  已发出全屏意图通知            ← 系统立刻执行，抢先启动面板
21:15:00.234  解锁 | 用途: 独立自检          ← 它按「自检模式」在跑
21:15:03~06   数字键盘 6/6 → 解锁成功        ← 解锁其实完全成功
21:15:06.369  结论：✔ 自动解锁成功           ← 但它只写报告就收工，不回调执行链
21:16:15.166  结果：✘ 失败 —— 解锁超时       ← 执行链白等 75 秒
```

修复三处：
1. **调整顺序** —— 先直接 `startActivity`，2.5 秒后确认没起来才动用全屏意图通知兜底
2. 全屏意图的 Intent **补上 `EXTRA_FOR_TASK`**
3. `WakeActivity` **重写 `onNewIntent`** —— `CLEAR_TOP|SINGLE_TOP` 复用实例时新 Intent 只能从这里拿到，
   以前不重写，标志永远更新不了

### 坑 5：卡死的任务静默吞掉用户的所有后续操作（v1.2 修复）

任务卡住时，用户点的每一次「立即执行 / 完整链路测试」都被 `已有任务在执行，忽略本次请求` 吞掉，
只有一句一闪而过的 toast —— 现象上就像「这些功能根本没实现」。

修复：任务空闲超过 3 分钟自动判定卡死并重置；界面新增「取消当前任务」按钮；
再次点击执行时会弹窗询问「取消并重新开始」；取消后有 3 秒冷却，避免旧任务的残留回调干扰新任务。

### 坑 6：全屏意图通知用固定 id，第二次就不再触发（v1.4 修复）

```java
nm.notify(2001, n);   // ❌ 固定 id，而且发完从不撤销
```

Android 里**同一个 id 的 `notify()` 是「更新」已有通知，不保证重新触发全屏意图**。所以：

| | 通知栏里 2001 的状态 | 结果 |
|---|---|---|
| 第一次执行 | 不存在 → 全新通知 | 全屏意图正常触发，面板被唤起 ✅ |
| 第二次执行 | 还挂着（从不撤销）→ 变成"更新" | 全屏意图不触发，只亮屏、面板起不来 ❌ |

现象就是「第一次能自动解锁，第二次只亮屏」。修复：**每次唤醒先撤掉上一条通知，再换一个新的 id 发**。

### 坑 7：唤醒只试一次就认命（v1.4 修复）

唤醒是整条链最脆的一环（后台启动 Activity 的限制因 ROM 而异）。v1.4 改成**三次尝试**，
每种打法都试一遍，并在日志里记清是哪一次成功的：

```
尝试 1/3：直接 startActivity
尝试 2/3：全屏意图通知（撤旧通知 + 新 id 重发）      ← 2.5 秒后仍未起来才走这步
尝试 3/3：再次直接 startActivity                    ← 7 秒后仍未起来才走这步
12 秒仍未起来 → 明确失败（不再干等 75 秒），并 dump 现场
```

### 坑 8：两次执行间隔太短，唤醒被「复用的旧实例」静默吞掉（v1.5 修复）

启动面板用的是 `FLAG_ACTIVITY_CLEAR_TOP | FLAG_ACTIVITY_SINGLE_TOP`。如果上一次的 `WakeActivity`
实例**还没被销毁**（间隔太短时完全可能），系统不会新建实例，而是复用旧实例并回调 `onNewIntent`。

而 v1.4 的 `onNewIntent` 只有两个分支：升级 `forTask` 标志、或「已解锁则补发回调」。
旧实例已经跑完（`done=true`）、设备又是锁屏状态时，**两个分支都不成立 → 什么也不做**。
于是这次唤醒被静默吞掉：面板不出现、不报错、`onCreate` 也不触发，任务只能干等到超时。

修复：`onNewIntent` 增加第三个分支 —— **复用了已结束的实例且设备仍锁定 → 重置面板、重新排看门狗、
重新执行整轮解锁流程**。同时把「点亮屏幕后立刻拉面板」改成**等 1.2 秒让锁屏界面稳定下来再拉**。

> 经验：所有「靠复用 Activity 实例」的设计，都必须显式处理"实例还活着但已经跑完"这个状态。

### 坑 9：昵称为空 → 盲点第一行 → **发错人**（v1.6 修复，事故级）

v1.5 实战记录：

```
好友昵称: (未设置，按置顶取最上面一条)      ← 昵称是空的
  #1 昵称="" bounds=[0,221][1080,405]
  #2 昵称="" ...
结果：✔ 成功 —— 成功给「置顶好友」发送了：111   ← 它自己也不知道发给了谁
```

两个问题叠加：

1. **空昵称时退化成「取列表第一行」** —— 而会话列表顺序会随新消息变化，
   「第一行」并不等于「置顶好友」。
2. **流程只校验"消息发出去了"，不校验"发给了谁"** —— 于是它把一次发错人报告成了"成功"。

修复（三道闸门）：

| 闸门 | 做法 |
|---|---|
| 入口 | **没有昵称直接拒绝执行**（`TaskRunner.start` 与界面按钮双重拦截），并推送一条说明通知 |
| 匹配 | `findTargetRow` **只认昵称匹配**，匹配不到返回 null，绝不猜 |
| 发送前 | `currentChatLooksLike()` —— 确认聊天窗口里确实能找到目标昵称，否则**中止，一个字都不发** |

同时把昵称匹配从"双向 contains"改成保守策略（见 `nicknameMatches`）：
短字符串不再互相包含，避免昵称填得短就命中一堆人。

### 坑 10：任务"复活"并行执行，导致乱发消息（v1.6 修复，事故级）

```java
if (running) {
    if (idle < 180_000L) { /* 拒绝 */ return; }
    running = false;        // ← 旧任务挂在 Handler 上的回调没被清掉
    releaseScreenWakelock();
}
// 紧接着启动新任务 → running 又变回 true
// → 旧任务那些 "if (!running) return" 的回调全部复活，与新流程并行点击同一个界面
```

现象就是「任务完成后还在跑，又跑去乱发消息」。

修复：引入**任务代次（generation）**。每次 `start()` 递增代次，所有延时回调都带上自己出发时的代次，
只有代次仍然相等才允许继续 —— 旧的残留回调永远无法影响新任务。同时卡死重置后**不再自动接力**，只提示用户重新发起。

### 坑 11：手动测试都过、真·定时就挂 —— 后台启动 Activity 的"最近可见"豁免（v1.8 修复）

对比两次运行，代码路径完全相同，结果相反：

| | 22:05 成功（手动） | 22:09 失败（闹钟） |
|---|---|---|
| 触发 | 点按钮后 12 秒，App 刚在前台 | 闹钟触发，App 已几分钟没露面 |
| 唤醒锁 | 都成功点亮屏幕 | 同样点亮 |
| 全屏意图 | id=2001，`canUseFullScreenIntent=true` | id=2002，同样 true |
| 结果 | 发出后 **292ms** Activity 创建 | 发出后**永远没有** `onCreate` |

唯一差异是「App 最近有没有在前台露过面」—— 这正是 Android 后台启动 Activity
豁免条件里的「App 的 Activity 最近可见」。手动测试时满足，真·定时时不满足，
再叠加小米的后台弹出限制，于是被静默拒绝。

**修复：分四级升级 + 一条不需要 Activity 的备用通道**

```
① 0.15s 发全屏意图通知（此刻屏幕仍熄灭 —— 这是系统唯一保证「点亮屏幕并启动界面」的通道）
② 0.7s  直接 startActivity
③ 4s    自己点亮屏幕 + 再次 startActivity
④ 8s    再发一次全屏意图（新 id）
⑤ 12s   放弃面板 → 走「不需要 Activity」的解锁通道：
          自己点亮屏幕 → 手势上滑唤出密码键盘 → 无障碍点数字 → 校验解锁
```

⑤ 是关键：`requestDismissKeyguard` 需要 Activity 上下文，但**锁屏密码键盘本身可以用无障碍
手势直接操作**，完全绕开最脆的"后台启动 Activity"环节。

### 坑 12：唤醒失败也去锁屏，用户看到「我刚解开它又锁上」（v1.8 修复）

`finish()` 里无条件执行 `pressHome() + lockScreen()`，任务失败时也照做。
现在改成**只有本次任务自己解锁过，才负责把屏幕锁回去**（`weUnlocked` 标志）。

### 坑 13：锁屏 10 秒自动息屏 —— 整条链在跟秒表赛跑（v1.9 修复）

用户实测反馈：**这台手机的锁屏点亮后 10 秒无操作就自动息屏**。这一条直接推翻了"屏幕会一直亮着"的前提：

| 环节 | 耗时 | 10 秒限制的后果 |
|---|---|---|
| 唤醒尝试阶梯 | 0 → 11 秒 | 前两次尝试屏幕还亮，到第 3、4 次时**屏幕已经自己灭了**，全打在黑屏上 |
| 解锁流程 | ~6 秒 | 勉强够，但任何延迟都会踩线 |
| **多闪阶段** | **10 秒以上** | 打开 App + 等列表 + 进会话 + 输入 + 发送 + 校验 → **跑到一半屏幕就灭了** |

而且更隐蔽的一点：**无障碍注入的点击手势不算"用户操作"，不会重置息屏计时**。所以
"有时候成、有时候卡在中间"就是这么来的。

**为什么 WakeLock 救不了**：实测 `SCREEN_BRIGHT_WAKE_LOCK | ACQUIRE_CAUSES_WAKEUP`
只能**点亮**屏幕，**并不阻止屏幕自动息屏**（10 秒后照样灭）。而且已经持有的锁再次 acquire
不会重新点亮 —— 必须「先 release 再 acquire」才能再次触发亮屏。

**修复：`ScreenKeeper`** —— 一个 1×1 像素、全透明、`FLAG_NOT_TOUCHABLE` 的
`TYPE_APPLICATION_OVERLAY` 悬浮窗，加上 `FLAG_KEEP_SCREEN_ON`。
整个任务期间屏幕都不会熄，而且不遮挡、不吃触摸、不影响无障碍读节点。只需要
「显示在其他应用上层」权限（已申请）。

### 坑 14：自己先点亮屏幕，反而废掉了全屏意图（v1.9 修复）

全屏意图（full-screen intent）**只有在屏幕仍熄灭时**才会「点亮屏幕并启动界面」；
屏幕已经亮着时，系统只把它当普通高优先级通知弹个横幅，**不启动界面**。

v1.4 我把全屏意图推迟到第 4 秒才发 —— 那时屏幕早就被我们自己的唤醒锁点亮了，
于是它彻底失效。这解释了「v1.1 闹钟触发能成功、v1.4 之后闹钟触发必失败」。

修复后的顺序：**趁屏幕还黑先把全屏意图打出去**，点亮屏幕推迟到第 3.5 秒的尝试 3。

---

## v2.0：多好友支持

配置从"单个昵称"改成"**一行一个昵称**"的列表（也接受逗号/顿号分隔）。执行流程：

```
唤醒 + 解锁
  └─ 好友 1：回到消息列表 → 按昵称命中会话行 → 进入 → 【校验顶栏是本人】→ 填文案 → 发送 → 回读校验
       └─ 随机等 6~14 秒（模拟真人节奏）
            └─ 好友 2：按返回键退到消息列表 → 重新按昵称定位 → …（列表顺序会变，所以每次都重新找）
                 └─ …直到处理完 → 汇总「成功 N / M 位」
```

多好友引入的三条新规则：

| 规则 | 原因 |
|---|---|
| **每位好友独立走完整校验** | 连续发送时任何一次串行都可能发错人。顶栏校验是**逐位**做的，不是做一次就信任全程 |
| **找不到就跳过，不中断整批** | 某位好友改了昵称、或昵称没暴露，只影响他自己；其余照发。结果里会写明「✘ 找不到这位好友」 |
| **每位之间随机停 6~14 秒** | 短时间连发是机器人最明显的特征，也是给系统喘息、避免点击过密被吞 |

另外 `isMessagePage` 现在**先排除聊天页**再判断 —— 实测聊天页打开后，会话列表连同顶栏标题
都还留在同一棵节点树里；不做这个排除，多好友连续发送时会把聊天页误判成列表页，
直接在错误的页面上找会话行。

### v2.1：好友之间"就地切换"，不再绕一趟桌面

早期版本对每一位好友都调用 `stepNormalizeDuoshan()`，而它的第一步是 `pressHome()` ——
于是每位好友都要「回桌面 → 冷启动多闪」，又慢又难看。

现在按起点是否明确分成两种模式：

| 场景 | 动作 |
|---|---|
| **第一位好友**（刚解锁，不知道前台是什么） | 回桌面清场 → 用 CLEAR_TOP 拉起多闪到根页面 |
| **后续好友**（我们已经在多闪的聊天页里） | **只按一次返回键**退出会话回到消息列表，就地继续 |

判据是 `stepNormalizeDuoshan(boolean freshStart)`。后续好友省掉了「回桌面 + 重启多闪」，
每位少花 3~5 秒，而且屏幕上不会再出现桌面闪烁。

---

## v2.2：修掉两个"看起来能跑、其实会认错人"的判据

这两个都是**测试全绿但逻辑是错的**，靠真实 dump 里的坐标数据才看出来。

### ① 昵称提取选错了节点

多闪既给头像节点设了名字（`D="好友戊"`），也给**会话行本身**设了整行摘要
（`D="好友戊,消息预览14:20"`）。摘要的面积是 1080×184，头像只有 144×144 ——
「取面积最大」于是选中了整行摘要。

它之所以还能匹配成功，是因为用了"包含"比对。但摘要里**混着消息预览**：

| 目标 | 摘要 | contains 结果 |
|---|---|---|
| `好友戊` | `好友戊,消息预览14:20` | ✓ 正确命中 |
| `RJ` | `…,RJ 在吗…`（别人发的消息里提到 RJ） | ✓ **误命中 → 发错人** |

修复：`rowNickname()` **跳过行节点自身，只在子节点里找名字**。实测 11 行的头像
全都带着干净的名字，所以这一改就能稳定拿到 `好友戊`、`好友甲` 这样的真名。

兜底再加一层：整行摘要的前两个字段（摘要格式是 `[置顶|免打扰,]名字,预览…`）
做**完全相等**比对 —— 只有整段文字恰好就是名字时才认。

### ② 收件人校验区的范围太宽

发送前会检查"聊天窗口顶栏是不是这个人"。原来的判据是「顶栏区域（屏幕顶部 10%）+ 文本匹配」，
但实测**消息列表一直铺到 y=0**，顶部那片区域里混着被顶栏盖住的消息气泡文字：

```
TextView id/o9e T="好友甲"        [254,118][407,168]  ← 真标题（y=118）
TextView id/c18 T="好友丁"  [589,185][822,225]  ← 一条消息气泡（y=185）
TextView id/kae T="16:57"           [487,0][593,-359]   ← 已滚出屏幕（高度为负）
```

10% 的阈值是 240，把 y=185 那条**消息气泡**也算了进去 —— 这道校验等于形同虚设
（只是"附近有人提过这个名字"就放行）。

修复：只认**顶部 6%（y ≤ 144）且尺寸有效**的节点。这样三条里只留下真标题。

### v2.3：改用「可聚焦」这一个本质判据

v2.2 的位置阈值仍然切不干净。第二轮的实测日志里，顶栏字符串还是混着杂音：

```
「1 / 好友戊」                        ← 「1」是消息或未读红点
「某条消息 / 1 / 好友丙」 ← 「某条消息」是一条消息
```

把坐标挖出来才看清问题所在：

| 节点 | 位置 | 可聚焦 |
|---|---|---|
| `id/o9e T="好友丙"` | [285,**138**][551,188] | **是（f）** |
| `T="某条消息"`（消息气泡） | [198,**4**][424,66] | 否 |
| 上一轮的 `T="好友丁"`（消息气泡） | [589,**185**][822,225] | 否 |

**消息列表一直铺到 y=0，气泡既可能出现在标题上方（y=4）也可能在下方（y=185）** ——
位置阈值无论怎么调都会有漏网的。真正的区别是：**聊天页标题是可聚焦节点，消息气泡和未读红点都不是。**

所以判据改成「可聚焦 + 尺寸有效 + 位于顶部 10%」。这是本质区别，不是调参。

> 取舍说明：这道闸门宁可**误拦**（跳过这位好友，日志里写明）也不能**误放**
> （把消息发给陌生人）。所以校验失败时会把顶栏区域所有文本连坐标一起打进日志，
> 方便下一轮直接看出判据该怎么调。

---

## v2.4：界面重做 + 权限清理

### 界面

从"一堆系统默认控件堆在滚动条里"改成卡片式布局（仍然零第三方依赖，全靠自绘 drawable + 样式）：

- **渐变头部**：应用名 + 动态副标题（"每天 01:00 自动给 4 位好友续火花"）+ 运行状态药丸 + 主开关
- **下次执行卡片**：大号时间 + 好友数/文案池/机型，下面是主次按钮（立即执行 / 完整链路测试 / 取消）
- **环境自检卡片**：8 项，每项是「彩色圆点 + 名称 + 一句人话解释 + 一键跳设置」，顶部有 `6 / 7 项就绪` 汇总
- **设置卡片**：每个输入框都有标题和示例占位符
- **最近一次执行卡片**：等宽字体结果块 + 分享诊断包
- **高级 / 诊断卡片**：默认折叠，把唤醒诊断、界面侦察、运行日志都收进去

顺手修的两个显示问题：

| 问题 | 原因 | 处理 |
|---|---|---|
| 内容会被状态栏盖住 | targetSdk 35+ 在 Android 15+ **强制边到边**，去掉 ActionBar 后内容从 y=0 开始 | 根布局加 `fitsSystemWindows="true"` |
| 头部开关"橙上加橙"看不见 | 开关轨道色和渐变头部都是橙色 | 头部开关单独用一套配色（白色轨道 + 橙色滑块） |

踩到的构建坑：样式名里的点号（`Btn.Primary`）会被 AAPT 当成**隐式父样式**去查找 `Btn`，
找不到就报 `resource style/Btn not found`。改用无点号的命名（`BtnPrimary`）。

另一个坑（真机闪退）：把布局里的 `<CheckBox>` 换成 `<Switch>` 时，Java 里的字段还声明着
`CheckBox` —— 两者是**兄弟类**，`findViewById` 强转直接抛 `ClassCastException` 闪退。
字段类型改成父类 `CompoundButton`，以后再换控件也不会崩。

> 经验：改布局控件类型时，务必同时检查 Java 侧字段声明的类型。
> 用父类型（`View` / `CompoundButton` / `TextView`）声明字段能免疫这类改动；
> 或者反过来，写个脚本把「布局里每个 id 的实际类名」和「Java 里字段声明的类型」对一遍。

### 权限

删掉了 `BIND_ACCESSIBILITY_SERVICE` 的 `<uses-permission>` 声明 —— 那是系统持有的权限，
只需要写在服务的 `android:permission` 属性上。现在声明 **11 条**，其中
`USE_EXACT_ALARM`/`SCHEDULE_EXACT_ALARM` 与 `FOREGROUND_SERVICE`/`FOREGROUND_SERVICE_SPECIAL_USE`
各是同一概念的新旧两种写法，实际概念 **9 个**。

---

## 核心设计原则：不假设任何起点

这是踩过的坑，单独记下来。

**错误做法**（v1 早期版本）：
```java
if (!isDuoshanForeground(s)) { s.openApp(PKG); }   // 只判断「要不要启动」
```
只判断了「多闪在不在前台」，没判断「它停在哪一页」。而 Android 拉起一个**已在运行**的 App 时，
默认是**恢复它上次的界面**，不是回到首页。场景：昨天执行完停在某个聊天页 → 今天解锁后多闪还在那个聊天页
→ 代码认为"已在前台"直接开始找会话列表 → 把消息气泡误判成会话项 → 轻则失败、重则发错人。

**正确做法**：开工前先**归零**，用循环纠错代替一次性判断。

```
每 1 秒一轮，最长 40 秒：
  ① 有开屏弹窗 / 活动页      → 关掉
  ② 前台不是多闪             → CLEAR_TOP 重新拉起（强制回根页面）
  ③ 在多闪但停在聊天页       → 按返回键退出（最多 4 次）
  ④ 连续 3 轮识别不出页面    → 按一次返回兜底（覆盖各种二级页面）
  ⑤ 确认在会话列表页且有目标会话 → ✔ 才开始点击
```

两端的对称设计：
- **开工前不假设**（上面这套归零循环）
- **收尾时恢复干净**（回桌面 + 锁屏），让下一次的起点可预期

**两条防误发的硬规则**（比成功率更重要）：
1. 填了好友昵称 → **只认昵称匹配**，匹配不到就失败退出，**绝不退化成"取最上面一条"**。
2. 检测到当前在聊天页时，不把页面里的消息气泡当成会话列表项。

---

## 架构

```
FireAlarmReceiver          setAlarmClock 到点触发（穿透 Doze）
   └─ FireService          前台服务保活，防止息屏后被冻结
   └─ TaskRunner           状态机：整条执行链 + 每步自动 dump 现场
        ├─ 第 0 步 唤醒+解锁 → WakeActivity（点亮屏幕 / 请求解锁 / 点数字键盘）
        ├─ 第 1 步 状态归零 → 关弹窗 / CLEAR_TOP 重拉 / 聊天页按返回，确认落在会话列表页才开始动手
        ├─ 第 2 步 进入会话 → 置顶好友 = 列表区最上面那一项
        ├─ 第 3 步 填文案   → ACTION_SET_TEXT
        ├─ 第 4 步 发送     → 文案匹配 / 输入框右侧可点击节点
        ├─ 第 5 步 回读校验 → 界面上能找到刚发的那句话
        └─ 收尾：回桌面 + 锁屏 + 写报告
```

| 文件 | 职责 |
|---|---|
| `FireScheduler.java` | 每日 `setAlarmClock` 排定 / 取消 / 描述下次触发 |
| `FireAlarmReceiver.java` | 闹钟到点 → 起服务 + 跑任务 + 滚动排定下一天 |
| `BootReceiver.java` | 开机 / 应用更新后恢复定时 |
| `FireService.java` | 前台服务（防冻结保活） |
| `TaskRunner.java` | 执行链状态机 + 每步 dump |
| `DuoshanDriver.java` | 多闪界面操作（文字优先定位） |
| `WakeActivity.java` | 亮屏 + 解锁面板（解锁后回调执行链） |
| `FireAccessibilityService.java` | 无障碍：节点收集、点击、坐标手势、数字键盘输入 |
| `NodeDump.java` | 节点树导出（紧凑 / 完整） |
| `Notifier.java` | 通知通道 + 全屏意图（系统级亮屏通道） |
| `ExportProvider.java` / `Exporter.java` | 零依赖文件分享（长文本无损） |
| `WakeTest.java` | 四段式唤醒诊断（回归验证用） |
| `Recon.java` | 延时采集当前界面 |
| `Keyguard` / `FireLog` / `Prefs` | 锁屏状态 / 日志 / 配置 |

---

## 编译

```powershell
$env:JAVA_HOME='<Android Studio 自带的 jbr 目录>'   # 必须是 JBR 21，工程要求 JDK 21
cd <项目目录>
.\gradlew.bat assembleDebug
```

| 组件 | 版本 |
|---|---|
| AGP / Gradle | 9.3.0 / 9.5.0 |
| SDK | `D:\<SDK 所在盘符>\AndroidSDK`（android-36.1 + build-tools 36.0.0） |
| compileSdk / targetSdk / minSdk | 36.1 / 36 / 30 |
| 语言 | Java 11 |

---

## 使用

1. 安装 APK（覆盖安装即可）。
2. 打开 App，确认 ① 区全部绿灯（无障碍 / 通知 / 全屏通知 / 悬浮窗 / 电池 / 精确闹钟）。
3. ② 区填写：**开启开关**、时间 `01:00`、好友昵称（置顶那位）、6 位锁屏密码、文案池（一行一条，每次随机取一条）→ 点「保存并排定闹钟」。
4. 点「立即执行（跳过解锁）」先验证多闪那半段；再用「完整链路测试」验证含亮屏解锁的整条链。
5. 失败时点「分享执行诊断包」——里面含任务报告 + 每一步的界面 dump，是修正定位规则的唯一依据。

---

## 已知待精确化的地方（v1-alpha）

会话列表项、输入框、发送按钮目前用的是**启发式规则**（列表区最上方一项 / 最靠下的可编辑节点 / 输入框右侧可点击节点），
首次运行可能选错目标。任务每一步都会把现场 dump 存成 `step_XX_*.txt`，
拿到真实 dump 后即可把规则替换成精确匹配（文字锚点 + 坐标兜底）。

---

## 风险与边界

- 安全锁屏下用系统 API 直接解锁无 root 无解；本项目的做法是用无障碍点击锁屏数字键盘绕过，已实测可行。
- 风控：每天只发 1 次、走正常前台 GUI 路径、文案随机化。不建议提高频率。
- 保活：国产 ROM 后台冻结是这类项目最常见的失败原因；「后台弹出界面」+ 自启动 + 省电策略无限制 三项缺一不可。
